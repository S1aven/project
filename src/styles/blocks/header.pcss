.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  min-height: 110px;
  padding: 0 160px;
}

.header__btns {
  margin: 0 20px;
}

.header__logo {
  line-height: 30px;
  // color: #ffffff;
  font-family: "Open Sans";
  font-weight: 800;
  font-size: 36px;
}